\section{Results}
\label{sec:results}

\TODO{This section is written after the runs. The structure below is fixed in advance
so that the analysis cannot be steered by the data; each subsection names the
hypothesis it tests and the figure that carries it.}

\subsection{Architectural versus textual effect sizes (\hyp{h:arch})}
\TODO{Figure 2: effect size per dimension, architectural group against textual group,
with bootstrap 95\% intervals. The headline number is the ratio of the two group
means.}

\subsection{Separating discovery from access (\hyp{h:reach})}
\TODO{Table 4: the four cells; $\Delta_{\text{access}}$ and
$\Delta_{\text{discovery}}$ with intervals. Include the pilot's $B$--$D$ contrast as
the historical bound.}

\subsection{Where sandboxed access wins (\hyp{h:lossy})}
\TODO{Figure 3: \metric{requirement\_f1} by access condition, split by decomposable
versus join-requiring fields.}

\subsection{Affordance use (\hyp{h:afford})}
\TODO{Figure 4: tool-call distribution with and without \texttt{list}/\texttt{read}
available; memory reuse rate across phases.}

\subsection{Misinformation absorption (\hyp{h:pollute})}
\TODO{Table 5: absorption rate normalized by exposure, by access condition.}

\subsection{Optima under seeding (\hyp{h:inert}, \hyp{h:cross})}
\TODO{Figure 5: $N^{*}$ and $Q^{*}$ curves, seeded against control, overlaid.}

\subsection{Key--judge agreement}
\TODO{Rate and direction of divergence between \metric{requirement\_f1} and
\metric{task\_utility}; inter-judge agreement per judged metric.}
