\section{Conclusion}

Public and private agent networks differ on the edge, not on visibility: an edge that
forms unilaterally and carries no consequence against one that requires assent and can be
recovered against. Three things follow---who can be reached, what an edge carries, what is
asserted about it---and comparisons of the two regimes move all three at once.

Separated on one kernel, one runtime and one model, the three weigh roughly $0.232$,
$0.080$ and $0$ on a hidden-profile task family. The formation advantage is reach: it
vanishes when the roster already holds what a task needs. The semantics advantage is real
but charged at 11k tokens an episode. The attribute axis does not move at all, and a
length-matched placebo of two neutral lines is indistinguishable from an assertion of
prior trust.

Two results we did not design say more than the ones we did. A bounded roster sees a
quarter as much misinformation and writes more of it into its deliverable, fabricating
values 58 times where open arms fabricated once: the filter that keeps bad claims out
removes the second source that catches them. And one brokered hop recovers a large part of
the gap while every assertion of trust stays flat---an introduction changes the graph, an
endorsement does not.

The weights were measured on a task family that asks for reach and offers nothing to
co-edit. Whether they are constants of the framework or of the task is the next
experiment, and it is the one the framework exists to specify: if the enterprise setting
does not move the weight on semantics, the combination problem was never a combination
problem. For an agentic web the implication is narrow and, we think, useful. Changing what
a network permits changes what the collective does. Describing the relationship does
not---not at the layer where descriptions currently live.
