\section{Experimental design}
\label{sec:design}

\subsection{Predictions}

Each prediction was chosen against one filter: \emph{would a careful reader bet on it
in advance?} Claims that pass trivially---an open network reaches more knowledge, a
persistent team completes more---are excluded, because confirming them consumes a
study and returns nothing. Every prediction below was fixed before the corresponding
sweep ran, together with the condition that would falsify it.

\begin{table}[t]
\centering\small
\caption{Predictions, fixed before the runs, with the outcome. Two survived, one was
falsified in a way that changed the paper's second finding, and one was withdrawn.}
\label{tab:hyp}
\begin{tabular}{@{}clp{5.4cm}l@{}}
\toprule
& \textbf{Prediction} & \textbf{Falsified if} & \textbf{Outcome} \\
\midrule
H1 & Edge formation dominates edge attributes & attribute contrasts move outcomes comparably to formation & \textbf{held} \\
H2 & Attributes are inert once text length is controlled & a relational seed beats a length-matched placebo consistently & \textbf{held} \\
H3 & Sandboxed edges beat readable ones on accuracy & readable edges are at least as accurate & \textbf{falsified} \\
H3$'$ & Sandboxed edges win \emph{per token} & readable edges are more efficient per token in either arm & \textbf{held} \\
H4 & The formation advantage is a reach advantage, and shrinks when reach stops being scarce & the gap is flat in $E$ and across beats & \textbf{held} \\
H5 & Readable edges absorb more misinformation & absorption tracks exposure alone & \textbf{withdrawn}, underpowered \\
\bottomrule
\end{tabular}
\end{table}

H3 is worth naming as a failure. We predicted that a counterpart answering from its own
full context would beat a requester re-joining fragments it had read---that extraction
is lossy in a way delegation is not. On accuracy it is not: readable edges are ahead in
both arms. The prediction survives only once cost is counted, which is a different and
weaker claim, and we report it as H3$'$ rather than presenting the original as
confirmed.

\subsection{Conditions}

Four cells of the $2\times2$, each a configuration of the same kernel: open or bounded
edge formation crossed with sandboxed or readable edge semantics. Cells $B$ and $C$ are
what allow the two main effects to be identified rather than summed; without them, an
open-versus-bounded contrast reports $\Delta_\text{formation} + \Delta_\text{semantics}$
and cannot say which term carried it.

The attribute axis holds the configuration fixed and varies only what is asserted in
context, across five relational profiles and a length-matched placebo.

\subsection{Controls}

One model across all agents and all cells, one runtime, one kernel; identical task
prompts, deliverable schemas, scoring functions, token budgets and seed distributions.
Rate limits and loop bounds are logged per episode rather than absorbed into results.
Rubrics, schemas, normalization rules and the scorer signature were frozen before any
run (Appendix~\ref{app:controls}).

Two controls deserve naming because each removed an explanation that would otherwise
have survived into the paper. The \textbf{requester keeps its own notes in every
cell}: withholding them from the open configurations would have manufactured the
warm-start result, so what differs is not memory but whether memory is bilateral. And
the \textbf{length-matched placebo} carries the same line count, position and register
as the relational seeds with no relational content, which is what separates an effect
of the relationship from an effect of the tokens describing it.
